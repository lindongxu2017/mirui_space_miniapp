<navigator wx:for="{{enterpriseArr}}" wx:key="{{index}}" hover-class='none' url="/pages/roomDisplay/roomDisplay?id={{item.album_type_id}}&title={{item.name}}">
	<image mode="widthFix" src="{{item.img_url}}"></image>
	<view>{{item.name}}</view>
</navigator>
<back wx:if="{{is_share}}"></back>